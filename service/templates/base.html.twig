<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="{{ path('home') }}">ShetCode</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        {% if app.session.get('user_id') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('problems_list') }}">Problems</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('my_drafts') }}">My Drafts</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('feedback') }}">Feedback</a>
                            </li>
                            <li class="nav-item">
                                <span class="nav-link text-light">Welcome, {{ app.session.get('username') }}</span>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-danger" href="{{ path('logout') }}">Logout</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('home') }}">Home</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        <main class="container py-4">
            {% block body %}{% endblock %}
        </main>

        <footer class="bg-light py-4 mt-5">
            <div class="container text-center">
                <p class="text-muted">ShetCode &copy; {{ "now"|date("Y") }}</p>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html> 